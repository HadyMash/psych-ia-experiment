import 'package:flutter/material.dart';

class ReadingText extends StatelessWidget {
  const ReadingText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
